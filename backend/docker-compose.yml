services:
  mongo:
    image: mongo:7
    container_name: mdesign-mongo
    ports:
      - "27017:27017"
    volumes:
      - mdesign-mongo:/data/db

  redis:
    image: redis:7-alpine
    container_name: mdesign-redis
    ports:
      - "6379:6379"

volumes:
  mdesign-mongo: